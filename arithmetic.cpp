#include <iostream>

int main(){
    //binary Oprators
    // both sides have value
    //minus a - b || 5 - 2 = 3;
    //plus a + b || 5 + 2 = 7;
    //multiple a * b || 5 * 2 = 10;
    //divid a / b || 5/2 = 2.5; 
    //modulus a % b || 5/2 = 1; modulus used to get remnder
    int i;
    int j;
    int k;
     i=1;
     j=2;
     //here i and j called as oprentes and these are not effect my opration we just get new value and store in new variable
k=i%j;
// in C division always trunkates mean get the whole number remove frational part
k=i%j;
std ::cout<<k;
}